version: v2
modules:
  - path: api/proto
lint:
  use:
    - STANDARD
  except:
    # Сервис называется MessengerService, RPC — императивы без суффиксов.
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
