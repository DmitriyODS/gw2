version: v2
modules:
  - path: api/proto
lint:
  use:
    - STANDARD
  except:
    # Сервис называется CallService, RPC — императивы без суффиксов.
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
