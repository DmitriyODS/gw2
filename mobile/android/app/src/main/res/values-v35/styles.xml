<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 15+ (targetSdk 35) принудительно включает edge-to-edge — WebView
         уезжает под статус-бар и нижнюю навигацию. Явный opt-out возвращает
         обычные непрозрачные системные панели: веб-шапка приложения не
         перекрывается. Атрибут помечен deprecated, но это официальный escape
         hatch; при его удалении из платформы перейти на
         adjustMarginsForEdgeToEdge в capacitor.config.json. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@color/splashBackground</item>
        <item name="android:navigationBarColor">@color/splashBackground</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@color/splashBackground</item>
        <item name="android:statusBarColor">@color/splashBackground</item>
        <item name="android:navigationBarColor">@color/splashBackground</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
