<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link возврата OAuth Яндекса: авторизация идёт в системном
                 браузере, страница /yandex-callback зовёт
                 groovework://yandex-callback?code=… — обрабатывает MainActivity. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="groovework" android:host="yandex-callback" />
            </intent-filter>

            <!-- Системное «Поделиться» текстом ИЛИ любыми файлами (в т.ч.
                 несколькими) → открыть приложение, выбрать получателя и
                 отправить в чат вложением (обрабатывает MainActivity). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Свой FCM-сервис ВМЕСТО сервиса плагина push-notifications (наш —
             его наследник, токены/JS-события доезжают через super). Нужен ради
             звонков: pushsvc шлёт их data-only + high priority, система трей
             сама не показывает — уведомление входящего звонка строим здесь,
             даже когда приложение убито. -->
        <service
            android:name=".PushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />

        <!-- Держит звонок живым при блокировке экрана/в Doze (partial wake lock);
             тип microphone — звонок использует микрофон. -->
        <service
            android:name=".CallForegroundService"
            android:foregroundServiceType="microphone"
            android:exported="false" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Пуш-уведомления (Android 13+, runtime-запрос делает веб-слой через
         Capacitor PushNotifications.requestPermissions). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Звонок при заблокированном экране: foreground-сервис (тип microphone),
         wake lock, полноэкранное уведомление входящего поверх локскрина,
         Bluetooth-гарнитура как аудиомаршрут. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Обновление обёртки из «О приложении»: скачанный APK ставится
         системным установщиком (AppUpdater). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Звонки в WebView: getUserMedia требует разрешений приложения, их
         пробрасывает BridgeWebChromeClient. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
</manifest>
