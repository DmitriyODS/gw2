/* Что в этом выпуске показываем, а что придерживаем.
 *
 * Флаги здесь — НЕ настройки пользователя и не фичи компании: это состояние
 * самого продукта. Механизмы под ними реализованы целиком и остаются в коде —
 * скрыт только вход в них, поэтому включение обратно = смена значения здесь
 * (плюс парная константа на бэкенде, см. ниже). */

/* Подписки, тарифы, витрина и оплата по СБП не успели к выпуску 7.0. Раздел
 * «Магазин», пилюля подписки в аккаунте и всё, что ведёт к оплате, скрыты;
 * токены ИИ на это время выдаются всем поровну — по 1000 в сутки.
 *
 * Парная константа бэкенда — domain.SubscriptionsHidden в billingsvc
 * (back-go/billing/internal/domain/limits.go): она же задаёт суточную квоту. */
export const SUBSCRIPTIONS_VISIBLE = false

/* Выбор модели ИИ временно закрыт — все работают на модели платформы (GPT).
 * Каталог моделей и личная настройка остаются; на СВОЁМ ключе выбор доступен,
 * потому что там платит сам пользователь.
 *
 * Парная константа бэкенда — domain.ModelChoiceLocked в aisvc; он же присылает
 * в настройках флаг model_locked, так что фронт полагается на ответ сервера,
 * а не на эту константу. */
export const AI_MODEL_CHOICE_VISIBLE = false
